# Frontend Placeholder

Member 3 owns the browser UI and WebSocket client.

Suggested first files to add:

- `frontend/index.html`
- `frontend/src/main.ts`
- `frontend/src/canvas.ts`
- `frontend/src/socket.ts`

The shared stroke payload shape is already defined in `shared/src/types.ts`.
